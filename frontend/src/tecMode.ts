// Which TEC map the globe shows. A single tri-state, shared by the panel switch
// and the globe: 'off' (no overlay), 'predicted' (coming soon), 'actual' (data).
export type TecMode = 'off' | 'predicted' | 'actual';
