# TEC map data source (Cloudflare R2). Optional.
# If unset, the app uses the production custom domain (https://data.ionocast.com).
#
# For local dev against the free r2.dev bucket (rate-limited, dev only):
# VITE_TEC_BASE_URL=https://pub-61b65ffdfdea42728421ca7d002690f8.r2.dev
#
# For local dev against the production data domain:
# VITE_TEC_BASE_URL=https://data.ionocast.com
